# Queries (manual mongosh snippets)

Эти файлы содержат команды для ручного выполнения в `mongosh` (или можно запустить их как `--file`).

Подразумевается, что вы подключились к MongoDB и находитесь в `shop` (либо выполните `00-use-shop.js`).

Папки соответствуют пунктам задания.
